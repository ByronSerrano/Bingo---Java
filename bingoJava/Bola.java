public class Bola {

    public static int numAleatorio(){
        int numero = (int)(Math.random()*90+1);
        return numero;
    }
    public static int numAleatorio45(){
        int numero = (int)(Math.random()*45+1);
        return numero;
    }
}
